require 'spec_helper'

describe LazyFormat do
  it 'has a version number' do
    expect(LazyFormat::VERSION).not_to be nil
  end

  it 'does something useful' do
    expect(false).to eq(true)
  end
end
